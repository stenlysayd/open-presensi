name: open_presensi_client
description: "Klien Flutter untuk Open Presensi - Sistem Presensi & Manajemen Komunitas."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  http: ^1.2.1
  shared_preferences: ^2.2.3
  mobile_scanner: ^5.1.1
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
